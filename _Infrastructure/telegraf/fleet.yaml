defaultNamespace: telegraf
helm:
  chart: telegraf
  repo: https://helm.influxdata.com/
  releaseName: telegraf
  version: ^1.8.23
  valuesFiles: 
    - values.yaml
  # valuesFrom:
  # - secretKeyRef:
  #     name: secret-values
  #     namespace: telegraf
  #     key: values.yaml